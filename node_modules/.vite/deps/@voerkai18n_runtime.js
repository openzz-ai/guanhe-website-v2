import {
  __require
} from "./chunk-DGUM43GV.js";

// node_modules/@voerkai18n/runtime/dist/index.mjs
var it = Object.defineProperty;
var kt = (e, t, r) => t in e ? it(e, t, { enumerable: true, configurable: true, writable: true, value: r }) : e[t] = r;
var u = (e, t) => it(e, "name", { value: t, configurable: true });
var M = ((e) => typeof __require != "undefined" ? __require : typeof Proxy != "undefined" ? new Proxy(e, { get: (t, r) => (typeof __require != "undefined" ? __require : t)[r] }) : e)(function(e) {
  if (typeof __require != "undefined") return __require.apply(this, arguments);
  throw new Error('Dynamic require of "' + e + '" is not supported');
});
var m = (e, t, r) => (kt(e, typeof t != "symbol" ? t + "" : t, r), r);
function h(e) {
  return e && typeof e == "function";
}
u(h, "t");
((e) => typeof M < "u" ? M : typeof Proxy < "u" ? new Proxy(e, { get: (t, r) => (typeof M < "u" ? M : t)[r] }) : e)(function(e) {
  if (typeof M < "u") return M.apply(this, arguments);
  throw new Error('Dynamic require of "' + e + '" is not supported');
});
function d(e) {
  if (typeof e != "object" || e === null) return false;
  var t = Object.getPrototypeOf(e);
  if (t === null) return true;
  for (var r = t; Object.getPrototypeOf(r) !== null; ) r = Object.getPrototypeOf(r);
  return t === r;
}
u(d, "o");
function y(e, ...t) {
  if (t.length === 0) return e;
  let r = t.map((s, i) => {
    let a = Object.entries(s || {});
    return a.some(([o, n]) => n === void 0) ? a.reduce((o, [n, l]) => (l !== void 0 && (o[n] = l), o), {}) : s;
  });
  return Object.assign(e, ...r);
}
u(y, "u");
function jt(e) {
  return d(e) ? ["$merge", "$ignoreUndefined"].some((t) => t in e) : false;
}
u(jt, "p");
function I(...e) {
  if (e.length < 2) throw new Error("deepMerge函数至少需要两个参数");
  let t = e.length > 0 ? jt(e[e.length - 1]) : false, { $merge: r, $ignoreUndefined: s, skipKeys: i } = y({ skipKeys: [], $ignoreUndefined: true, $merge: "replace" }, t ? e[e.length - 1] : {});
  function a(o, n) {
    d(o) && Object.entries(o).forEach(([l, f]) => {
      let c;
      l in n ? Array.isArray(f) && Array.isArray(n[l]) ? (r === "replace" ? c = f : r === "append" ? c = [...n[l], ...f] : r === "append" ? c = [.../* @__PURE__ */ new Set([...n[l], ...f])] : typeof r == "function" && (c = r(n[l], f, { key: l, from: o, to: n })), n[l] = s && f === void 0 ? n[l] : c) : d(f) && d(n[l]) ? a(f, n[l]) : n[l] = s && f === void 0 ? n[l] : f : n[l] = f;
    });
  }
  return u(a, "l"), e.reduce((o, n, l) => l == 0 ? o || {} : (!d(n) || t && l === e.length - 1 || a(n, o || {}), o), e[0]);
}
u(I, "h");
function x(e, t = false) {
  if (typeof e == "number") return true;
  if (typeof e != "string" || t) return false;
  try {
    if (e.includes(".")) {
      let r = parseFloat(e);
      return e.endsWith(".") ? !isNaN(r) && String(r).length === e.length - 1 : !isNaN(r) && String(r).length === e.length;
    } else {
      let r = parseInt(e);
      return !isNaN(r) && String(r).length === e.length;
    }
  } catch (r) {
    return false;
  }
}
u(x, "r");
function ot(e) {
  return e === null ? "Null" : e === void 0 ? "Undefined" : typeof e == "function" ? "Function" : e.constructor && e.constructor.name;
}
u(ot, "getDataTypeName");
async function at(e, t) {
  let r = await t.call(e);
  return "__esModule" in r || Symbol.toStringTag in r ? r.default : r;
}
u(at, "loadAsyncModule");
function nt(e, t, r) {
  for (; e.indexOf(t) > -1; ) e = e.replace(t, r);
  return e;
}
u(nt, "replaceAll");
function T(e) {
  return e.replace(/([.?*+^$[\]\\(){}|-])/g, "\\$1");
}
u(T, "escapeRegexpStr");
var ut = ["String", "Number", "Boolean", "Object", "Array", "Function", "Error", "Symbol", "RegExp", "Date", "Null", "Undefined", "Set", "Map", "WeakSet", "WeakMap"];
function B(e) {
  try {
    return x(e) ? parseFloat(e) : 0;
  } catch (t) {
    return e;
  }
}
u(B, "toNumber");
function z(e) {
  try {
    return e instanceof Date ? e : new Date(e);
  } catch (t) {
    return parseInt(e);
  }
}
u(z, "toDate");
function Ce(e) {
  return !!e;
}
u(Ce, "toBoolean");
function lt() {
  return Date.now().toString() + parseInt(String(Math.random() * 1e3));
}
u(lt, "randomId");
var W = { en: { month: { long: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"], short: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sept", "Oct", "Nov", "Dec"] }, weekday: { long: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], short: ["Sun", "Mon", "Tues", "Wed", "Thur", "Fri", "Sat"] } }, cn: { month: { long: ["一月", "二月", "三月", "四月", "五月", "六月", "七月", "八月", "九月", "十月", "十一月", "十二月"], short: ["1月", "2月", "3月", "4月", "5月", "6月", "7月", "8月", "9月", "10月", "11月", "12月"] }, weekday: { long: ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"], short: ["周日", "周一", "周二", "周三", "周四", "周五", "周六"] } } };
var ft = { en: { slots: [12], names: ["AM", "PM"] }, cn: { slots: [6, 9, 11, 13, 18], names: ["凌晨", "早上", "上午", "中午", "下午", "晚上"] } };
var Pt = "YYYY-MM-DD HH:mm:ss";
function ct(e) {
  e < 0 && e > 23 && (e = 0);
  let t = [0, ...ft[this.language].slots, 24].findIndex((r) => r > e) - 1;
  return ft[this.language].names[t];
}
u(ct, "h");
function V(e, t, r) {
  let s = y({ language: "cn" }, r), i = e instanceof Date ? e : new Date(e || Date.now()), a = i.getHours(), o = String(a).padStart(2, "0"), n = a > 12 ? a - 12 : a, l = String(n).padStart(2, "0"), f = String(i.getMinutes()), c = String(i.getSeconds()), g = String(i.getMilliseconds()), p = String(i.getFullYear()), _ = i.getMonth(), b = i.getDay(), F = String(i.getDate()), R = [["HH", o], ["H", a], ["hh", l], ["h", n], ["mm", f.padStart(2, "0")], ["m", f], ["ss", c.padStart(2, "0")], ["s", c], ["SSS", g.padStart(3, "0")], ["A", a > 12 ? "PM" : "AM"], ["a", a > 12 ? "pm" : "am"], ["t", ct.call(s, a)], ["T", ct.call(s, a)], ["YYYY", p], ["yyyy", p], ["YY", p.substring(2)], ["MMM", W[s.language].month.short[_]], ["MM", String(_ + 1).padStart(2, "0")], ["M", _ + 1], ["DD", F.padStart(2, "0")], ["D", F], ["d", b], ["dd", W[s.language].weekday.short[b]], ["ddd", W[s.language].weekday.short[b]], ["dddd", W[s.language].weekday.long[b]]], D = t || Pt;
  return R.forEach(([Y, w]) => D = D.replaceAll(Y, w)), D;
}
u(V, "H");
function Rt(e, t, r) {
  if (typeof t == "string") {
    let s = 0, i;
    for (; (i = e.indexOf(t, s)) > -1; ) {
      let a = typeof r == "function" ? r(t) : r, o = e.length;
      e = e.substring(0, i) + a + e.substring(i + t.length), s = i + a.length + e.length - o;
    }
  } else {
    let s;
    if (!t.global || !t.multiline) throw new Error("The search parameter must be enabled '/gm' option");
    for (; (s = t.exec(e)) !== null; ) {
      s.index === t.lastIndex && t.lastIndex++;
      let i = e.length, a = s[0].length, o = typeof r == "function" ? r(s[0], ...s) : r;
      e = e.substring(0, s.index) + o + e.substring(s.index + a), t.lastIndex += e.length - i;
    }
  }
  return e;
}
u(Rt, "s");
String.prototype.replaceAll || (String.prototype.replaceAll = function(e, t) {
  return Rt(this, e, t);
});
var O = [1e3, 6e4, 36e5, 864e5, 6048e5, 2592e6, 31536e6, Number.MAX_SAFE_INTEGER];
function U(e, t, r) {
  let { units: s, now: i, before: a, after: o } = y({ units: ["秒", "分钟", "小时", "天", "周", "个月", "年"], now: "刚刚", before: "{value}{unit}前", after: "{value}{unit}后" }, r), n = typeof e == "number" ? new Date(e).getTime() : e.getTime(), l = (t instanceof Date ? t : typeof t == "number" ? new Date(t) : /* @__PURE__ */ new Date()).getTime(), f = n - l, c = O.findIndex((g) => Math.abs(f) < g) - 1;
  if (c < 0 && (c = 0), c > O.length - 1 && (c = O.length - 1), f < 1e3 && f > -1e3) return i;
  if (f <= -1e3) {
    let g = parseInt(String(Math.abs(f) / O[c]));
    return a.replace("{value}", g).replace("{unit}", s[c]);
  } else {
    let g = parseInt(String(Math.abs(f) / O[c]));
    return o.replace("{value}", g).replace("{unit}", s[c]);
  }
}
u(U, "b");
function K(e) {
  return e != null && typeof e[Symbol.iterator] == "function" && typeof e != "string";
}
u(K, "t");
var Yt = /(\w*)\[\s*(\d)\s*\]/;
var gt = u(class extends Error {
}, "c");
function Ht(e) {
  let t = e.match(Yt);
  if (t) {
    let r = t[1], s = parseInt(t[2]);
    return [r, s];
  }
}
u(Ht, "P");
function S(e, t, r) {
  let { defaultValue: s, matched: i, ignoreInvalidPath: a } = y({ ignoreInvalidPath: true, defaultValue: void 0 }, r);
  for (; t.includes("]["); ) t = t.replace("][", "].[");
  let o = t.split("."), n = e, l = null, f = -1, c = false;
  for (let g = 0; g < o.length; g++) {
    let p = o[g];
    if (p in n) l = n, f = p, n = n[p];
    else if (p.trim().endsWith("]")) {
      let _ = Ht(p);
      if (_) {
        let [b, F] = _;
        if (b in n && (n = n[b]), Array.isArray(n)) l = n, f = F, n = n[F];
        else if (K(n) && typeof F == "number") l = n, f = F, n = [...n][F];
        else {
          c = true;
          break;
        }
        continue;
      }
      c = true;
      break;
    } else c = true;
    if (n == null) {
      g < o.length && (c = true);
      break;
    }
  }
  if (c) if (a) n = s;
  else throw new gt();
  return typeof i == "function" && !c && i({ value: n, parent: l, indexOrKey: f }), n;
}
u(S, "k");
function C(e) {
  return e ? !!(Array.isArray(e) || e instanceof Set || e instanceof Map || d(e)) : false;
}
u(C, "e");
var Bt = /\n/gm;
var zt = /(["'])(.*?)(\1)/gm;
var Wt = /([\s\[\,\{])(\w+)(\s*\:)/gm;
function q(e, t) {
  try {
    return JSON.parse(e, (s, i) => t ? t(s, i) : i);
  } catch (s) {
  }
  let r = e.replace(Bt, (s, i, a) => {
    let o = a.slice(0, i).trim(), n = a.slice(i + 1).trim();
    return !/,$/.test(o) && !/[\[\{\}]$/.test(o) && !/^\}/.test(n) ? `,
` : `
`;
  });
  return r = r.replaceAll(zt, (s, i, a) => `"${encodeURI(a)}"`), r = r.replaceAll(Wt, (s, i, a, o) => `${i}"${a}"${o}`), r = r.replaceAll("，", ",").replaceAll("“", '"').replaceAll("”", '"'), JSON.parse(r, (s, i) => (typeof i == "string" && (i = decodeURI(i)), t ? t(s, i) : i));
}
u(q, "g");
function ht(e) {
  return e ? e.trim().substring(1).trim().split("|").map((r) => r.trim()).map((r) => {
    if (r == "") return null;
    let s = r.indexOf("("), i = r.lastIndexOf(")");
    if (s !== -1 && i !== -1) {
      let a = r.substring(s + 1, i).trim(), o = Gt(a);
      return [r.substring(0, s), o];
    } else return [r, []];
  }).filter((r) => Array.isArray(r)) : [];
}
u(ht, "parseFormatters");
function Vt(...e) {
  e.length == 0 && (e.push(["{", "}"]), e.push(["[", "]"]));
  let t = e.map(([r, s]) => `(${T(r)}1%.*?%1${T(s)})`);
  return qt.replace("__TAG_REGEXP__", t.length > 0 ? t.join("|") + "|" : "");
}
u(Vt, "getNestingParamsRegex");
function Jt(e, t = "{", r = "}") {
  let s = 0, i = 0;
  for (; s < e.length; ) {
    let a = e.slice(s, s + t.length), o = e.slice(s, s + r.length);
    if (a == t) {
      i++, e = e.substring(0, s + t.length) + `${i}%` + e.substring(s + t.length), s += t.length + String(i).length + 1;
      continue;
    }
    if (o == r) {
      i > 0 && (e = e.substring(0, s) + `%${i}` + e.substring(s)), s += r.length + String(i).length + 1, i--;
      continue;
    }
    s++;
  }
  return e;
}
u(Jt, "addTagFlags");
function Ut(e, ...t) {
  return t.length == 0 && (t.push(["{", "}"]), t.push(["[", "]"])), t.forEach((r) => {
    e.includes(r[0]) && e.includes(r[1]) && (e = Jt(e, ...r));
  }), e;
}
u(Ut, "addTagHelperFlags");
function Kt(e, t, r) {
  return [[t, new RegExp(T(t) + "\\d+%")], [r, new RegExp("%\\d+" + T(r))]].forEach(([i, a]) => {
    let o;
    for (; (o = a.exec(e)) !== null; ) o.index === a.lastIndex && a.lastIndex++, e = e.replace(a, i);
  }), e;
}
u(Kt, "removeTagFlags");
function G(e, ...t) {
  return t.length == 0 && (t.push(["{", "}"]), t.push(["[", "]"])), t.forEach(([r, s]) => {
    e.includes(r) && e.includes(s) && (e = Kt(e, r, s));
  }), e;
}
u(G, "removeTagHelperFlags");
var qt = String.raw`((([\'\"])(.*?)\3))|__TAG_REGEXP__([\d]+\.?[\d]?)|((true|false|null)(?=[,\b\s]))|([\w\.]+)|((?<=,)\s*(?=,))(?<=\s*[,\)]?\s*)`;
function Gt(e) {
  let t = [], r;
  e = Ut(e);
  try {
    let s = new RegExp(Vt(), "g");
    for (; (r = s.exec(e)) !== null; ) {
      r.index === s.lastIndex && s.lastIndex++;
      let i = r[0];
      if (i.trim() == "") i = null;
      else if (i.startsWith("'") && i.endsWith("'") || i.startsWith('"') && i.endsWith('"')) i = i.substring(1, i.length - 1), i = G(i);
      else if (i.startsWith("{") && i.endsWith("}") || i.startsWith("[") && i.endsWith("]")) try {
        i = G(i), i = q(i);
      } catch (a) {
      }
      else ["true", "false", "null"].includes(i) ? i = JSON.parse(i) : x(i) ? i = parseFloat(i) : i = G(String(i));
      t.push(i);
    }
  } catch (s) {
  }
  return t;
}
u(Gt, "parseFormaterParams");
function Qt(e, t, r) {
  let s = y({ normalize: null, params: null, configKey: null }, t), i = u(function(a, o, n) {
    let l = a;
    if (h(s.normalize)) try {
      l = s.normalize(l);
    } catch (g) {
    }
    let f = y({}, r, S(n, s.configKey, { defaultValue: {} })), c;
    if (s.params == null) c = o;
    else {
      c = s.params.map((g) => {
        let p = S(f, g, void 0);
        return h(p) ? p(l) : p;
      });
      for (let g = 0; g < c.length; g++) o[g] !== void 0 && (c[g] = o[g]);
    }
    return e.call(this, l, c, f);
  }, "$formatter");
  return i.$name = t == null ? void 0 : t.configKey, i.configurable = true, i;
}
u(Qt, "createFormatter");
var Xt = u(function(e, t, r) {
  let s = y({ params: {} }, t);
  return L(function(a, o, n) {
    let l = (t.params || {}).reduce((f, c) => (f[c] = n[c] == null ? (r || {})[c] : n[c], f), {});
    if (o.length == 1 && d(o[0])) Object.assign(l, o[0]);
    else for (let f = 0; f < o.length; f++) o[f] !== void 0 && (l[s.params[f]] = o[f]);
    return e.call(this, a, l, n);
  }, { ...t, params: null });
}, "createFlexFormatter");
var L = Qt;
var pt = Xt;
function Zt(e, t = "HH:mm:ss") {
  return V(e, t, {});
}
u(Zt, "formatTime");
function N(e = {}, t) {
  let r = y({ presets: {} }, e);
  return L(function(s, [i], a) {
    if (i in r.presets && h(r.presets[i])) return r.presets[i](s);
    if (i in a) i = a[i];
    else if (i == "number") return s;
    try {
      return i == null ? s : t.call(this, s, i);
    } catch (o) {
      return s;
    }
  }, r);
}
u(N, "createDateTimeFormatter");
var mt = N({ normalize: z, params: ["format"], configKey: "datetime.date", presets: { local: (e) => e.toLocaleString(), iso: (e) => e.toISOString(), utc: (e) => e.toUTCString(), gmt: (e) => e.toGMTString() } }, V);
var dt = N({ normalize: (e) => {
  let t = e.getMonth() + 1;
  return Math.floor(t % 3 == 0 ? t / 3 : t / 3 + 1);
}, params: ["format"], configKey: "datetime.quarter" }, (e, t) => t[e - 1]);
var yt = N({ normalize: (e) => e.getMonth() + 1, params: ["format"], configKey: "datetime.month" }, (e, t) => t[e - 1]);
var _t = N({ normalize: (e) => e.getDay(), params: ["format"], configKey: "datetime.weekday" }, (e, t) => t[e]);
var bt = N({ normalize: z, params: ["format"], configKey: "datetime.time", presets: { local: (e) => e.toLocaleTimeString(), timestamp: (e) => e.getTime() } }, Zt);
var Ft = L((e, [t], r) => U(e, t, r), { normalize: z, params: ["base"], configKey: "datetime.relativeTime" });
function te(e, t = 3) {
  let r = new RegExp(String.raw`(?!^)(?=(\d{${t}})+$)`, "g"), s = e.replace(r, ",");
  return s.startsWith(",") && (s = s.substring(1)), s.endsWith(",") && (s = s.substring(0, s.length - 2)), s;
}
u(te, "addSplitChars");
function Q(e, t = {}, r = {}) {
  let { symbol: s = "", division: i = 3, prefix: a = "", precision: o = 2, suffix: n = "", unit: l = 0, radix: f = 3, format: c = "{symbol}{value}{unit}" } = t, [g, p] = String(e).split(".");
  l > 0 && f > 0 && (g.length < f * l && (g = new Array(f * l - g.length + 1).fill(0).join("") + g), p = g.substring(Number(g), g.length - f * l) + p, g = g.substring(0, g.length - f * l), g == "" && (g = "0"));
  let _ = [];
  if (_.push(te(g, i)), p && x(o) && o != 0 && o != 0) {
    if (o == -1) _.push(`.${p}`);
    else if (o > 0) {
      p.length;
      o < 0 ? p = String(parseFloat(`0.${p}`)) : p = String(parseFloat(`0.${p}`).toFixed(o)).split(".")[1], _.push(`.${p}`);
    }
  }
  let b = S(r, "units", { defaultValue: [] })[l] || "";
  return c.replace("{value}", _.join("")).replace("{symbol}", s).replace("{prefix}", a).replace("{suffix}", n).replace("{unit}", b);
}
u(Q, "toCurrency");
var xt = pt((e, t = {}, r) => (t.format in r && (t.format = r[t.format]), t.unit = parseInt(t.unit) || 0, t.unit > r.units.length - 1 && (t.unit = r.units.length - 1), t.unit < 0 && (t.unit = 0), t.unit > 0 && t.precision == 2 && (t.precision = -1), Q(e, t, r)), { normalize: (e) => B(e), params: ["format", "unit", "precision", "prefix", "suffix", "division", "symbol", "radix"], configKey: "currency" }, { format: "default", unit: 0 });
var Et = L(function(e, [t, r], s) {
  return Q(e, { division: r, precision: t }, s);
}, { normalize: B, params: ["precision", "division"], configKey: "number" });
var St = { $config: { datetime: { units: ["Year", "Quarter", "Month", "Week", "Day", "Hour", "Minute", "Second", "Millisecond", "Microsecond"], date: { long: "YYYY/MM/DD HH:mm:ss", short: "YYYY/MM/DD", format: "local" }, quarter: { long: ["First Quarter", "Second Quarter", "Third Quarter", "Fourth Quarter"], short: ["Q1", "Q2", "Q3", "Q4"], format: "short" }, month: { long: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"], short: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sept", "Oct", "Nov", "Dec"], format: "long" }, weekday: { long: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], short: ["Sun", "Mon", "Tues", "Wed", "Thur", "Fri", "Sat"], format: "long" }, time: { long: "HH:mm:ss", short: "HH:mm:ss", format: "local" }, timeSlots: { slots: [12], lowerCases: ["am", "pm"], upperCases: ["AM", "PM"] }, relativeTime: { units: ["seconds", "minutes", "hours", "days", "weeks", "months", "years"], now: "Just now", before: "{value} {unit} ago", after: "after {value} {unit}" } }, currency: { default: "{symbol}{value}{unit}", long: "{prefix} {symbol}{value}{unit}{suffix}", short: "{symbol}{value}{unit}", custom: "{prefix} {symbol}{value}{unit}{suffix}", format: "default", units: ["", " thousands", " millions", " billions", " trillions"], radix: 3, symbol: "$", prefix: "USD", suffix: "", division: 3, precision: 2 }, number: { division: 3, precision: -1 } }, $types: { Null: (e) => "", Undefined: (e) => "", Error: (e) => "ERROR", Boolean: (e) => e ? "True" : "False" }, date: mt, quarter: dt, month: yt, weekday: _t, time: bt, relativeTime: Ft, currency: xt, number: Et };
var X = ["年", "季度", "月", "周", "日", "小时", "分钟", "秒", "毫秒", "微秒"];
var Z = ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"];
var tt = ["周日", "周一", "周二", "周三", "周四", "周五", "周六"];
var et = ["一月", "二月", "三月", "四月", "五月", "六月", "七月", "八月", "九月", "十月", "十一月", "十二月"];
var rt = ["1月", "2月", "3月", "4月", "5月", "6月", "7月", "8月", "9月", "10月", "11月", "12月"];
var Mt = { $config: { datetime: { units: X, date: { long: "YYYY年M月D日 HH点mm分ss秒", short: "YYYY年M月D日", format: "local" }, quarter: { long: ["一季度", "二季度", "三季度", "四季度"], short: ["Q1", "Q2", "Q3", "Q4"], format: "short" }, month: { long: et, short: rt, format: "long" }, weekday: { long: Z, short: tt, format: "long" }, time: { long: "HH点mm分ss秒", short: "HH:mm:ss", format: "local" }, timeSlots: { slots: [6, 9, 11, 13, 18], lowerCases: ["凌晨", "早上", "上午", "中午", "下午", "晚上"], upperCases: ["凌晨", "早上", "上午", "中午", "下午", "晚上"] }, relativeTime: { units: ["秒", "分钟", "小时", "天", "周", "个月", "年"], now: "刚刚", before: "{value}{unit}前", after: "{value}{unit}后" } }, currency: { units: ["", "万", "亿", "万亿", "万万亿"], radix: 4, symbol: "￥", prefix: "RMB", suffix: "元", division: 4, precision: 2 }, number: { division: 4, precision: -1 } }, $types: { Null: (e) => "", Undefined: (e) => "", Boolean: (e) => e ? "是" : "否" } };
var vt = { "*": {}, en: St, zh: Mt };
var E = "en";
var $t = [{ name: "zh", title: "中文", default: true, active: true }, { name: "en", title: "英文" }];
function Dt(e) {
  return e == null || e == null || typeof e == "string" || typeof e == "number" || typeof e == "boolean" || typeof e == "symbol" || typeof e == "bigint";
}
u(Dt, "t");
function v(e) {
  if (e == null || Dt(e) || h(e)) return e;
  if (Array.isArray(e)) return e.map((t) => C(t) ? v(t) : t);
  if (e instanceof Set) {
    let t = /* @__PURE__ */ new Set();
    for (let r of e.values()) C(r) ? t.add(v(r)) : t.add(r);
    return t;
  } else if (e instanceof Map) {
    let t = /* @__PURE__ */ new Map();
    for (let [r, s] of e.entries()) C(s) ? t.set(r, v(s)) : t.set(r, s);
    return t;
  } else if (typeof e == "object") {
    let t = {};
    return Object.entries(e).forEach(([r, s]) => {
      C(s) ? t[r] = v(s) : t[r] = s;
    }), t;
  } else return e;
}
u(v, "s");
var $ = class {
  constructor(t) {
    m(this, "_formatters", {});
    m(this, "_activeFormatters", {});
    m(this, "_activeFormattersConfigs", {});
    m(this, "_scope");
    m(this, "_language");
    m(this, "_formatterCache", { typedFormatters: {}, formatters: {} });
    this._scope = t;
  }
  get activeLanguage() {
    var t;
    return this._language || (this._language = ((t = this._scope) == null ? void 0 : t.activeLanguage) || "zh"), this._language;
  }
  get scope() {
    return this._scope;
  }
  async change(t) {
    var r, s, i, a, o, n, l;
    try {
      let f = t;
      if (t in this.formatters || (f = ((s = (r = this.scope) == null ? void 0 : r.getLanguage(t)) == null ? void 0 : s.fallback) || "zh", (i = this.scope) == null || i.logger.warn(`没有配置<${t}>格式化器，使用后退语言<${f}>替代.`)), f) {
        let c = this.formatters[f];
        h(c) ? this._activeFormatters = await at(this, c) : this._activeFormatters = c, this._formatterCache = { typedFormatters: {}, formatters: {} }, this.generateFormattersConfigs(f), this._language = t;
      } else (o = this.scope) == null || o.logger.warn(`未指定<${t}>格式化器配置(scope=${(a = this.scope) == null ? void 0 : a.id})`);
    } catch (f) {
      (l = this.scope) == null || l.logger.error(`当加载<${t}>格式化器时出错(scope=${(n = this.scope) == null ? void 0 : n.id}): ${f.stack}`);
    }
  }
  generateFormattersConfigs(t) {
    var r, s, i, a;
    try {
      let o = [], n = (s = (r = this.scope) == null ? void 0 : r.getLanguage(t)) == null ? void 0 : s.fallback;
      this.scope && (n && o.push(this.scope.global.formatters.getConfig(n)), o.push(this.scope.global.formatters.getConfig("en")), o.push(this.scope.global.formatters.getConfig(t))), o.push(this.getConfig("en")), n && o.push(this.getConfig(n)), o.push(this.getConfig(t)), this._activeFormattersConfigs = o.reduce((l, f) => (d(f) && (l = I(l, v(f), { $merge: "replace" })), l), {});
    } catch (o) {
      (i = this.scope) != null && i.debug && console.error(`当生成<${t}>格式化器配置时出错(scope=${(a = this.scope) == null ? void 0 : a.id})：${o.stack}`), this._activeFormattersConfigs = {};
    }
  }
  updateConfig(t, r) {
    if (t in this._formatters) {
      let s = this._formatters[t];
      "$config" in s || (s.$config = {}), y(s.$config, r);
    }
    t === this._language && this.generateFormattersConfigs(t);
  }
  registerLanguageFormatters(t, r) {
    this._formatters[t] = r;
  }
  loadInitials(t) {
    this._formatters = t;
  }
  register(t, r, s) {
    let { language: i = "*" } = s || {};
    if (!h(r) || typeof t != "string") throw new TypeError("格式化器必须是一个函数");
    (Array.isArray(i) ? i : i ? i.split(",") : []).forEach((o) => {
      if (o in this._formatters || (this._formatters[o] = {}), typeof this._formatters[o] != "function") {
        this._formatters[o] || (this._formatters[o] = {});
        let n = this._formatters[o];
        ut.includes(t) ? (n.$types || (n.$types = {}), n.$types[t] = r) : n[t] = r;
      }
    });
  }
  getConfig(t) {
    return t ? S(this._formatters, `${t}.$config`, { defaultValue: {} }) : {};
  }
  getTypes(t) {
    return t ? S(this._formatters, `${t}.$types`, { defaultValue: {} }) : {};
  }
  getFormatters(t) {
    return t ? S(this._formatters, t, { defaultValue: {} }) : {};
  }
  get formatters() {
    return this._formatters;
  }
  get activeFormatters() {
    return this._activeFormatters;
  }
  get config() {
    return this._activeFormattersConfigs;
  }
  get types() {
    return this._activeFormatters.$types;
  }
  get(t, r) {
    var n, l, f, c, g, p, _, b, F, R, D, Y;
    let { on: s, inGlobal: i } = y({ on: "scope", inGlobal: true }, r);
    if (s == "types" && t in this._formatterCache.typedFormatters) return this._formatterCache.typedFormatters[t];
    if (s == "scope" && t in this._formatterCache.formatters) return this._formatterCache.formatters[t];
    let a = (l = (n = this.scope) == null ? void 0 : n.getLanguage(this.activeLanguage)) == null ? void 0 : l.fallback, o = [];
    s == "types" ? (o.push(this.types), a && o.push(this.getTypes(a)), a != E && o.push(this.getTypes(E)), o.push(this.getTypes("*")), i && (o.push((f = this.scope) == null ? void 0 : f.global.formatters.types), a && o.push((c = this.scope) == null ? void 0 : c.global.formatters.getTypes(a)), a != E && o.push((g = this.scope) == null ? void 0 : g.global.formatters.getTypes(E)), o.push((p = this.scope) == null ? void 0 : p.global.formatters.getTypes("*")), o.push((_ = this.scope) == null ? void 0 : _.global.formatters.getTypes("en")))) : s == "scope" && (o.push(this._activeFormatters), a && o.push(this.getFormatters(a)), a != E && o.push(this.getFormatters(E)), o.push(this.getFormatters("*")), o.push(this.getFormatters("en")), i && (o.push((b = this.scope) == null ? void 0 : b.global.formatters.activeFormatters), a && o.push((F = this.scope) == null ? void 0 : F.global.formatters.getFormatters(a)), a != E && o.push((R = this.scope) == null ? void 0 : R.global.formatters.getFormatters(E)), o.push((D = this.scope) == null ? void 0 : D.global.formatters.getFormatters("*")), o.push((Y = this.scope) == null ? void 0 : Y.global.formatters.getFormatters("en"))));
    for (let w of o) if (w && d(w) && t in w) {
      let H = w[t];
      if (h(H)) return s == "types" ? this._formatterCache.typedFormatters[t] = H : this._formatterCache.formatters[t] = H, H;
    }
  }
};
u($, "VoerkaI18nFormatterRegistry");
var k = class extends Error {
};
u(k, "InvalidLanguageError");
var wt = u(class {
  constructor() {
    this._listeners = /* @__PURE__ */ new Map(), this._lastMessage = {};
  }
  get listeners() {
    return this._listeners;
  }
  on(e, t, r) {
    let { objectify: s = false, count: i = -1 } = Object.assign({}, r);
    this._listeners.has(e) || this._listeners.set(e, /* @__PURE__ */ new Map());
    let a = ++wt.listenerSeqId, o = this._listeners.get(e);
    return o == null || o.set(a, [t, i]), this.emitRetainEvent(e, a, o), s ? { off: () => {
      o == null || o.delete(a), (o == null ? void 0 : o.size) == 0 && this._listeners.delete(e);
    } } : a;
  }
  emitRetainEvent(e, t, r) {
    e in this._lastMessage && this.executeListener(t, r, this._lastMessage[e]);
  }
  once(e, t, r) {
    return this.on(e, t, Object.assign({}, r, { count: 1 }));
  }
  forEachListeners(e) {
    let t = false;
    for (let [r, s] of this._listeners.entries()) {
      if (t) break;
      for (let [i, [a, o]] of s.entries()) {
        if (t) break;
        t = e({ event: r, listenerId: i, listener: a, count: o, eventListeners: s }) === false;
      }
    }
  }
  forEachEventListeners(e, t) {
    let r = false, s = [[e, this._listeners.get(e)]];
    for (let [i, a] of s) if (a) for (let [o, [n, l]] of a) {
      if (r) break;
      r = t({ event: i, listenerId: o, listener: n, count: l, eventListeners: a }) === false;
    }
  }
  off() {
    if (arguments.length == 1) {
      if (typeof arguments[0] == "number") this.forEachListeners(({ listenerId: e, eventListeners: t, event: r }) => {
        var s;
        if (e == arguments[0]) return t.delete(e), ((s = this._listeners.get(r)) == null ? void 0 : s.size) == 0 && this._listeners.delete(r), false;
      });
      else if (typeof arguments[0] == "function") {
        let e = arguments[0];
        this.forEachListeners(({ listenerId: t, listener: r, eventListeners: s, event: i }) => {
          var a;
          r == e && (s.delete(t), ((a = this._listeners.get(i)) == null ? void 0 : a.size) == 0 && this._listeners.delete(i));
        });
      }
    } else arguments.length == 2 && typeof arguments[0] == "string" && typeof arguments[1] == "function" && this.forEachEventListeners(arguments[0], ({ event: e, listenerId: t, listener: r, eventListeners: s }) => {
      var i;
      e == arguments[0] && r == arguments[1] && (s.delete(t), ((i = this._listeners.get(e)) == null ? void 0 : i.size) == 0 && this._listeners.delete(e));
    });
  }
  waitFor(e, t = 0) {
    return new Promise((r, s) => {
      let i, a = false, o;
      t > 0 && (i = setTimeout(() => {
        a = true, this.off(o), s(new Error("Timeout"));
      }, t)), o = this.once(e, (n) => {
        a || (clearTimeout(i), r(n));
      });
    });
  }
  offAll(e) {
    e ? this._listeners.delete(e) : this._listeners.clear();
  }
  executeListener(e, t, r) {
    if (!t) return;
    let s = t.get(e);
    if (s) try {
      return s[0](r);
    } catch (i) {
    } finally {
      s[1] > -1 && (s[1] = s[1] - 1, s[1] == 0 && t.delete(e));
    }
  }
  executeListeners(e, t, r) {
    let s = [];
    return this.forEachEventListeners(e, ({ event: i, listenerId: a, eventListeners: o }) => {
      s.push(this.executeListener(a, o, t)), typeof r == "function" && r(a), o.size == 0 && this._listeners.delete(i);
    }), s;
  }
  emit(e, t, r) {
    return r && (this._lastMessage[e] = t), this.executeListeners(e, t);
  }
}, "f");
var J = wt;
J.listenerSeqId = 0;
var Lt = { get(e) {
  if (globalThis.localStorage) return globalThis.localStorage.getItem(e);
}, set(e, t) {
  globalThis.localStorage && globalThis.localStorage.setItem(e, t);
}, remove(e) {
  globalThis.localStorage && globalThis.localStorage.removeItem(e);
} };
var At = { warn: console.warn, error: console.error, info: console.info, debug: console.debug };
function It(e) {
  return Object.keys(At).reduce((t, r) => (t[r] = function(...s) {
    e && At[r]("[VoerkaI18n]", ...s);
  }, t), {});
}
u(It, "createLogger");
var ee = { debug: true, defaultLanguage: "zh", activeLanguage: "zh", storage: Lt, languages: [{ name: "zh", title: "中文", default: true }, { name: "en", title: "英文" }] };
var j = class extends J {
  constructor(r) {
    super();
    m(this, "_scopes", []);
    m(this, "_defaultMessageLoader");
    m(this, "_formatters", new $());
    m(this, "_appScopeId");
    m(this, "_options");
    m(this, "_logger");
    m(this, "_appInitilized", false);
    if (j.instance) return j.instance;
    this._options = I({}, ee, r), this._logger = It(this.options.debug), j.instance = this, this.loadInitialFormatters().then(() => {
      this.loadOptionsFromStorage();
    });
  }
  get debug() {
    return this.options.debug;
  }
  get options() {
    return this._options;
  }
  get logger() {
    return this._logger;
  }
  get scopes() {
    return this._scopes;
  }
  get appScopeId() {
    return this._appScopeId;
  }
  get activeLanguage() {
    return this.options.activeLanguage;
  }
  get defaultLanguage() {
    return this.options.defaultLanguage;
  }
  get languages() {
    return this.options.languages;
  }
  get defaultMessageLoader() {
    return this._defaultMessageLoader;
  }
  get formatters() {
    return this._formatters;
  }
  get storage() {
    return this.options.storage;
  }
  initApp(r) {
    if (this._appInitilized) {
      this.logger.warn("VoerkaI18n只允许注册一个library=false的i18nScope,请检查是否正确配置了library参数");
      return;
    }
    y(this.options, r), this.loadOptionsFromStorage(), this._appInitilized = true;
  }
  loadOptionsFromStorage() {
    if (this.options.storage) {
      let s = this.options.storage.get("language");
      s && !this.hasLanguage(s) && this.logger.warn("从存储中读取到未配置的语言名称参数：", s), s && this.logger.info("从存储中读取到当前语言名称参数：", s), s && s !== "undefined" && (this.options.activeLanguage = s, this.logger.debug("当前语言设置为：", s));
    }
  }
  saveOptionsToStorage() {
    if (this.options.storage) {
      let r = this.options.storage;
      if (!this.options.activeLanguage) return;
      r.set("language", this.activeLanguage), this.logger.debug("当前语言设置已保存到存储：", this.activeLanguage);
    }
  }
  async loadInitialFormatters() {
    this._formatters.loadInitials(vt), this._formatters.change(this.options.activeLanguage);
  }
  async loadMessagesFromDefaultLoader(r, s) {
    if (this._defaultMessageLoader && h(this._defaultMessageLoader)) try {
      return await this._defaultMessageLoader.call(s, r, s);
    } catch (i) {
      return this.logger.debug(`从远程加载语言包${r}文件出错:${i.stack}`), {};
    }
  }
  async change(r) {
    if (this.languages.findIndex((s) => s.name === r) !== -1 || h(this._defaultMessageLoader)) return this._formatters.change(r), await this._refreshScopes(r), this.options.activeLanguage = r, this.emit("change", r), this.saveOptionsToStorage(), this.logger.info("语言已切换为：", r), r;
    throw new k(r);
  }
  async _refreshScopes(r) {
    try {
      let s = this._scopes.map((i) => i.refresh(r));
      Promise.allSettled ? await Promise.allSettled(s) : await Promise.all(s);
    } catch (s) {
      this.logger.error("刷新语言作用域时出错:", s.stack);
    }
  }
  async register(r) {
    let s = this._scopes.length === 0 && !r.options.library;
    this._scopes.push(r), this._scopes.length === 1 && (this._appScopeId = r.id), r.options.library === false && (this._appScopeId = r.id), await r.refresh(this.activeLanguage), s && this.emit("ready", { language: this.activeLanguage, scope: r.id }, true);
  }
  async ready() {
    return await this.waitFor("ready");
  }
  registerFormatter(r, s, i) {
    let { language: a = "*" } = i || {};
    if (!h(s) || typeof r != "string") throw new TypeError("格式化器必须是一个函数");
    this._formatters.register(r, s, { language: a });
  }
  registerDefaultLoader(r) {
    if (!h(r)) throw new Error("默认语言加载器必须是一个函数");
    this._defaultMessageLoader = r;
  }
  async refresh() {
    return this._refreshScopes(this.activeLanguage);
  }
  clearPatchedMessages() {
    this._scopes.forEach((r) => r.clearPatchedMessages());
  }
  hasLanguage(r) {
    return this.languages.findIndex((s) => s.name == r) != -1;
  }
};
var A = j;
u(A, "VoerkaI18nManager"), m(A, "instance");
var P = /\{\s*(\w+)?((\s*\|\s*\w*(\(.*\)){0,1}\s*)*)\s*\}/g;
function Tt(e, t, r = {}) {
  let s = e, i, a = Object.assign({ replaceAll: true }, r);
  for (P.lastIndex = 0; (i = P.exec(s)) !== null; ) {
    i.index === P.lastIndex && P.lastIndex++;
    let o = i[1] || "", n = ht(i[2] || "");
    if (h(t)) try {
      let l = t(o, n, i[0]);
      a.replaceAll ? s = nt(s, i[0], l) : s = s.replace(i[0], l);
    } catch (l) {
      break;
    }
    P.lastIndex = 0;
  }
  return s;
}
u(Tt, "forEachInterpolatedVars");
function re(e, t, r, s) {
  if (t.length === 0) return e;
  let i = e;
  for (let a of t) try {
    i = a(i, r.formatters.config);
  } catch (o) {
    o.formatter = a.$name, r.logger.error(`当执行格式化器<${a.$name}>时出错: ${s},${o.stack}`);
  }
  return i;
}
u(re, "executeFormatter");
function se(e, t, r) {
  let s = [];
  for (let [i, a] of r) {
    let o = e.formatters.get(i, { on: "scope" }), n;
    h(o) ? n = u((l, f) => o.call(e.formatters.config, l, a, f), "formatter") : n = u((l) => h(l[i]) ? String(l[i](...a)) : String(l), "formatter"), n.$name = i, s.push(n);
  }
  return s;
}
u(se, "wrapperFormatters");
function Ot(e, t, r, s, i) {
  let a = s, o = se(e, t, r), n = e.formatters.get(ot(s), { on: "types" });
  return n && o.splice(0, 0, (l, f) => n.call(f, l, [], f)), re(a, o, e, i);
}
u(Ot, "getFormattedValue");
function Ct(e, ...t) {
  let r = this, s = r.global.activeLanguage;
  if (t.length === 1 && d(t[0])) {
    let i = t[0];
    return Tt(e, (a, o, n) => {
      let l = a in i ? i[a] : "";
      return Ot(r, s, o, l, e);
    });
  } else {
    let i = t.length === 1 && Array.isArray(t[0]) ? [...t[0]] : t, a = 0;
    return Tt(e, (o, n, l) => Ot(r, s, n, i.length > a ? i[a++] : void 0, e), { replaceAll: false });
  }
}
u(Ct, "replaceInterpolatedVars");
function ie(e) {
  return x(e);
}
u(ie, "isMessageId");
function oe(e, t) {
  try {
    return Array.isArray(e) ? e.length > t ? e[t] : e[e.length - 1] : e;
  } catch (r) {
    return Array.isArray(e) ? e[0] : e;
  }
}
u(oe, "getPluraMessage");
function Nt(e, ...t) {
  var o;
  let r = this, s = e, i = [], a = null;
  if (typeof e != "string") return e;
  try {
    if (arguments.length === 2 && d(arguments[1])) {
      let n = arguments[1];
      for (let [l, f] of Object.entries(n)) {
        if (h(f)) try {
          n[l] = f();
        } catch (g) {
          n[l] = f;
        }
        let c = typeof n[l] == "number";
        (a == null && c || l.startsWith("$") && c) && (a = n[l]);
      }
      i = [n];
    } else arguments.length >= 2 && (i = [...arguments].splice(1).map((n, l) => {
      try {
        n = h(n) ? n() : n, x(n) && (a = parseInt(n));
      } catch (f) {
        return String(n);
      }
      return n;
    }));
    if (ie(e)) s = r.current[e] || e;
    else {
      let n = r.idMap[e];
      s = (o = n ? r.current[n] : r.current[e]) != null ? o : e;
    }
    return Array.isArray(s) && s.length > 0 && (a !== null ? s = oe(s, a) : s = s[0]), t.length === 0 ? s : Ct.call(r, s, ...i);
  } catch (n) {
    return s;
  }
}
u(Nt, "translate");
var st = class {
  constructor(t) {
    m(this, "_options");
    m(this, "_global");
    m(this, "_refreshing", false);
    m(this, "_t");
    m(this, "_formatterRegistry");
    m(this, "_defaultLanguage");
    m(this, "_activeLanguage");
    m(this, "_currentMessages", {});
    m(this, "_patchedMessages", {});
    this._options = y({ id: lt(), library: false, debug: false, languages: [], messages: {}, idMap: {}, formatters: {} }, t), this.init(), this._global = this.registerToManager(), this._t = Nt.bind(this);
  }
  get options() {
    return this._options;
  }
  get id() {
    return this._options.id;
  }
  get logger() {
    return this._global.logger;
  }
  get debug() {
    return this._options.debug;
  }
  get defaultLanguage() {
    return this._global.defaultLanguage;
  }
  get activeLanguage() {
    return this._global.activeLanguage;
  }
  get default() {
    return this._options.messages[this._defaultLanguage];
  }
  get current() {
    return this._currentMessages;
  }
  get messages() {
    return this._options.messages;
  }
  get idMap() {
    return this._options.idMap;
  }
  get languages() {
    return this._options.languages;
  }
  get global() {
    return this._global;
  }
  get formatters() {
    return this._formatterRegistry;
  }
  get activeFormatters() {
    return this._formatterRegistry.formatters;
  }
  get t() {
    return this._t;
  }
  init() {
    if (!Array.isArray(this.languages)) this.logger.warn("无效的语言配置，将使用默认语言配置"), this._options.languages = Object.assign([], $t);
    else if (this.languages.length == 0) throw new Error("[VoerkaI18n]无效的语言配置，必须提供有效的默认语言和活动语言");
    let t, r;
    this.languages.forEach((i) => {
      i.fallback || (i.fallback = E), i.default && (r = i.name), i.active && (t = i.name);
    });
    let s = this._options.messages;
    if (r in s || (r = Object.keys(s)[0]), t in s || (t = r), !(r in s)) throw new Error("[VoerkaI18n]无效的语言配置，必须提供有效的默认语言和活动语言.");
    if (this._activeLanguage = t, this._defaultLanguage = r, h(this.messages[this._defaultLanguage])) throw new Error("[VoerkaI18n] 默认语言包必须是静态内容,不能使用异步加载的方式.");
    this._currentMessages = this.messages[this._activeLanguage], this.loadInitialFormatters();
  }
  registerToManager(t) {
    return globalThis.VoerkaI18n || (globalThis.VoerkaI18n = new A({ debug: this._options.debug, defaultLanguage: this._defaultLanguage, activeLanguage: this._activeLanguage, languages: this._options.languages, storage: this._options.storage })), this._options.library || globalThis.VoerkaI18n.initApp({ defaultLanguage: this._defaultLanguage, activeLanguage: this._activeLanguage, languages: this._options.languages, storage: this._options.storage, debug: this._options.debug }), this._global = globalThis.VoerkaI18n, this._global.register(this).then(() => {
      this.onRegisterSuccess.call(this), t == null || t.call(this);
    }).catch((r) => {
      this.onRegisterFail.call(this, r);
    }), this._global;
  }
  onRegisterSuccess() {
    typeof this._options.ready == "function" && this._options.ready.call(this), this.global.emit("registered", { language: this.activeLanguage, scope: this.id }), this._restorePatchedMessages(this._currentMessages, this.activeLanguage), this._patch(this._currentMessages, this.activeLanguage);
  }
  onRegisterFail(t) {
    typeof this._options.ready == "function" && this._options.ready.call(this, t);
  }
  registerFormatter(t, r, s) {
    let { language: i = "*", asGlobal: a = true } = s || {};
    a ? this.global.registerFormatter(t, r, { language: i }) : this.formatters.register(t, r, { language: i });
  }
  loadInitialFormatters() {
    this._formatterRegistry = new $(this), this.formatters.loadInitials(this._options.formatters), delete this._options.formatters;
  }
  registerDefaultLoader(t) {
    this.global.registerDefaultLoader(t);
  }
  getLanguage(t) {
    let r = this.languages.findIndex((s) => s.name == t);
    if (r !== -1) return this.languages[r];
  }
  hasLanguage(t) {
    return this.languages.findIndex((r) => r.name == t) != -1;
  }
  fallbackToDefault() {
    this._currentMessages = this.default, this._activeLanguage = this._defaultLanguage;
  }
  async loadLanguageMessages(t) {
    this.logger.debug(`准备加载语言包:${t}`);
    let r = this.messages[t], s = {};
    if (d(r)) s = r;
    else if (h(r)) {
      let i = await r.call(this);
      "__esModule" in i || Symbol.toStringTag in i ? s = i.default : s = i;
    } else if (h(this.global.defaultMessageLoader)) {
      let i = await this.global.loadMessagesFromDefaultLoader(t, this);
      d(i) && (d(i.$config) && (this.formatters.updateConfig(t, i.$config), delete i.$config), s = Object.assign({ $remote: true }, this.default, i));
    } else throw new Error(`没有为<${t}>指定语言包加载器`);
    return s;
  }
  async refresh(t) {
    if (this._refreshing = true, t || (t = this.activeLanguage), t === this._defaultLanguage) {
      this._currentMessages = this.default, this._restorePatchedMessages(this._currentMessages, t), await this._patch(this._currentMessages, t), await this.formatters.change(t), this._refreshing = false;
      return;
    } else try {
      let r = await this.loadLanguageMessages(t);
      r ? (this._currentMessages = r, this._activeLanguage = t, this._restorePatchedMessages(this._currentMessages, t), r.$remote || await this._patch(this._currentMessages, t), await this.formatters.change(t)) : (this.logger.warn(`无法加载语言包<${t}>(scope=${this.id}),将回退到默认语言`), this.fallbackToDefault());
    } catch (r) {
      this.logger.warn(`当加载语言包<${t}>时出错(scope=${this.id}): ${r.message}`), this.fallbackToDefault();
    } finally {
      this._refreshing = false;
    }
  }
  async _patch(t, r) {
    if (h(this.global.loadMessagesFromDefaultLoader)) try {
      let s = await this.global.loadMessagesFromDefaultLoader(r, this);
      d(s) && (Object.assign(t, s), this._savePatchedMessages(s, r), this._global.emit("patched", { language: r, scope: this.id }), this.logger.debug(`已更新了语言补丁包<${r}>(scope=${this.id}),共${Object.keys(s).length}条`));
    } catch (s) {
      this.logger.error(`当从远程加载语言补丁包<${r}>时出错:${s.stack}(scope=${this})`);
    }
  }
  _restorePatchedMessages(t, r) {
    let s = this._getPatchedMessages(r);
    d(s) && (Object.assign(t, s), this._global.emit("restore", { language: r, scope: this.id }), this.logger.debug(`成功恢复补丁语言包<${r}>(scope=${this.id})`));
  }
  _savePatchedMessages(t, r) {
    if (this.global.storage) try {
      this.global.storage.set(`voerkai18n_${this.id}_${r}_patched_messages`, JSON.stringify(t));
    } catch (s) {
      this.logger.error(`保存语言包补丁(${r})时出错:${s.stack}(scope=${this.id})`);
    }
  }
  clearPatchedMessages(t) {
    if (this.global.storage) {
      let r = t ? [t] : this.languages.map((s) => s.name);
      for (let s of r) this.global.storage.remove(`voerkai18n_${this.id}_${s}_patched_messages`);
    }
  }
  _getPatchedMessages(t) {
    try {
      return this.global.storage ? this.global.storage.get(`voerkai18n_${this.id}_${t}_patched_messages`) : {};
    } catch (r) {
      return this.logger.error(`读取语言包补丁(${t})时出错:${r.stack}(scope=${this.id})`), {};
    }
  }
  on(t, r) {
    return this._global.on(t, r);
  }
  once(t, r) {
    return this._global.once(t, r);
  }
  off(t, r) {
    return this._global.off(t, r);
  }
  async change(t) {
    return await this._global.change(t);
  }
  ready(t) {
    return this._global.ready().then(t);
  }
};
u(st, "VoerkaI18nScope");
export {
  ut as DataTypes,
  E as DefaultFallbackLanguage,
  $t as DefaultLanguageSettings,
  pt as FlexFormatter,
  L as Formatter,
  k as InvalidLanguageError,
  A as VoerkaI18nManager,
  st as VoerkaI18nScope,
  Xt as createFlexFormatter,
  Qt as createFormatter,
  T as escapeRegexpStr,
  ot as getDataTypeName,
  at as loadAsyncModule,
  ht as parseFormatters,
  lt as randomId,
  nt as replaceAll,
  Ce as toBoolean,
  z as toDate,
  B as toNumber,
  Nt as translate
};
//# sourceMappingURL=@voerkai18n_runtime.js.map
