/**
 *
 *   返回指定名称的方法是否是一个属性
 *  而不是一个函数
 *
 * @param inst
 * @param name
 * @returns {boolean}
 */
declare function isPropertyMethod(inst: object, name: string): false | ((v: any) => void) | undefined;

export { isPropertyMethod };
