/**
 * 判断值是否是一个数字
 *
 * 默认情况下，也会对字符串类型的数字进行判断
 * 如isNumber('123')返回true
 *
 *
 * @param {*} value
 * @returns
 */
declare function isNumber(value: any, strict?: boolean): boolean;

export { isNumber };
