/**
 * 不可重入
 * @param fn
 * @param options
 * @returns
 */
declare function memorize(fn: Function, options?: {
    hash?: ((args: any[]) => string) | 'length' | boolean;
    expires?: number;
}): Function;

export { memorize };
