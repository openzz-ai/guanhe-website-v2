/**
 * 向上查找指定的文件，并返回文件全路径
 *
 * findUpFiles("flex.config.json")
 * findUpFiles([
 *    "flex.config.json",
 *    "flex.config.yaml",
 *    "flex.config.ts"
 * ])
 *
 * 如果没有发现，则返回[]
 *
 *
 */
interface FindUpOptions {
    includeSelf?: boolean;
    entry?: string;
    onlyFirst?: boolean;
}
declare function findUp(files: string | string[], options?: FindUpOptions): string[];

export { type FindUpOptions, findUp };
