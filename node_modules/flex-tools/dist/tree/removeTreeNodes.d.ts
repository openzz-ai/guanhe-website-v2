import { TreeNodeOptions, TreeNodeBase, TreeNode } from './types.js';
import { I as IForEachTreeCallback } from '../forEachTree-D2aQzbAW.js';

/**
 *
 * 移除满足条件的节点
 *
 *
 * removeTreeNodes({
 *    id:1,
 *    children
 *
 * })
 *
 */

interface RemoveTreeNodesOptions extends TreeNodeOptions {
    onlyRemoveOne?: boolean;
}
declare function removeTreeNodes<Node extends TreeNodeBase = TreeNode>(treeObj: Node | Node[], matcher: IForEachTreeCallback<Node>, options?: RemoveTreeNodesOptions): void;

export { type RemoveTreeNodesOptions, removeTreeNodes };
