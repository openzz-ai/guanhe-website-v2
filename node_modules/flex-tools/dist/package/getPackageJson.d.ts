import { PackageJson } from 'type-fest';

/**
 *
 * @param entry
 * @param findUp   是否向上查找package.json，默认为true
 * @returns
 */
declare function getPackageJson(entry?: string, findUp?: boolean): PackageJson | undefined;

export { getPackageJson };
