interface PackageIsInstalledOptions {
    location?: string;
}
/**
 * 查询指定的包是否安装，如果安装则返回版本号
 * @param packageName
 */
declare function packageIsInstalled(packageName: string, options?: PackageIsInstalledOptions): Promise<boolean>;

export { type PackageIsInstalledOptions, packageIsInstalled };
