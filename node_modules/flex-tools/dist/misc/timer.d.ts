interface TimerOptions {
    begin(reset?: boolean): void;
    end(tips?: string, options?: {
        unit: string;
        log: boolean;
    }): number;
}
declare const timer: TimerOptions;

export { type TimerOptions, timer };
