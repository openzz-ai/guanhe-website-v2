declare global {
    interface String {
        ljust(width: number, fillChar?: string): string;
    }
}
declare function ljust(str: string, width: number, fillChar?: string): string;

export { ljust };
